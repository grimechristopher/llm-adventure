# Environment Configuration for LLM Adventure Game

# Quart Configuration
SECRET_KEY=your_secret_key_here
DEBUG=False
HOST=127.0.0.1
PORT=5000

# PostgreSQL Configuration (for chat history storage)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=llm_adventure

# Azure OpenAI Configuration (if using Azure)
AZURE_ONE_OPENAI_API_URL=https://your-resource.openai.azure.com/
AZURE_ONE_OPENAI_API_KEY=your_azure_api_key_here
AZURE_ONE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_ONE_OPENAI_API_VERSION=2024-02-15-preview

# LM Studio Configuration (if using local LLM)
# LM Studio runs on http://127.0.0.1:1234/v1 by default
# No configuration needed - just ensure LM Studio is running